# Für lokale Entwicklung: Sync zwischen Browsern / Geräten über die PHP-API.
# Kopie nach .env.local und Dev-Server neu starten (`npm run dev`).
# Ohne diese Variable: nur localStorage (pro Browser), kein fetch/save zur API.
VITE_API_URL=https://altbaufinder.markusgerke.com/api
